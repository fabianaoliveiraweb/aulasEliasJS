function mostrarDados() {
    const resp1 = prompt("Digite seu nome: ");
    const resp2 = prompt("Digite sua idade: ");

    const idade = parseInt(resp2);

    alert("Seja bem vindo " + resp1 + " que tem a idade " + resp2);
}